import "./Header.css";

const Header = () =>{
    return(
        <div className="h-screen bg-header relative flex items-center justify-between">
            
        </div>
    );
};

export default Header;